# `data/analysis`

Analysis-ready data, model inputs, model outputs, and derived tables are written here.

Most files in this directory are generated from restricted underlying data and are therefore ignored by Git.
