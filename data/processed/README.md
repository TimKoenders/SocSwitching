# `data/processed`

Locally generated intermediate data are written here.

Do not commit generated `.rds`, `.RData`, `.csv`, or other data objects unless the relevant source licenses explicitly permit redistribution and the project has decided to include them.
